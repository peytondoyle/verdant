# Uncomment to enable CI
# This workflow runs type checking and tests on pull requests
# To enable: remove the comment block around the entire workflow

# name: CI
# 
# on:
#   pull_request:
#     branches: [main, develop]
#   push:
#     branches: [main]
# 
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4
#       
#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 'lts/*'
#           cache: 'npm'
#       
#       - name: Install dependencies
#         run: npm ci
#       
#       - name: Type check
#         run: npm run typecheck
#       
#       - name: Run tests
#         run: npm test
#       
#       - name: Lint code
#         run: npm run lint
#       
#       # Uncomment if you want to run expo-doctor (requires EXPO_TOKEN secret)
#       # - name: Run Expo Doctor
#       #   run: npx expo-doctor
#       #   env:
#       #     EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
# 
# # To enable this workflow:
# # 1. Remove the comment blocks around the workflow
# # 2. Optionally add EXPO_TOKEN secret in GitHub repository settings
# # 3. Adjust branches and jobs as needed for your workflow